export interface userInterface {
  id: number; // Identificador
  name?: string; // nombre de usuario
  rol?: string; // rol del usuario
  email?: string; // especialidad del usuario
  create_at?: string;
  updated_at?: Date;
}